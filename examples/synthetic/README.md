# Synthetic Demo Fixture

This folder describes the built-in public demo event used by `marketbrief load-demo-data`
and `marketbrief run-agent-pipeline --demo`.

The company, ticker, event, price rows, research sources, and rejected social signal are
fictional. They exist so the project can demonstrate source-grounded generation without
redistributing paid provider data, generated media, or private API responses.
