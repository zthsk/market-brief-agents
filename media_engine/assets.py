from __future__ import annotations

from media_engine import paths


AUDIO_ROOT = paths.AUDIO_ROOT
ASSET_ROOT = paths.ASSET_ROOT
BRAND_ROOT = paths.BRAND_ROOT
DATA_ROOT = paths.DATA_ROOT
LOGO_PATH = paths.LOGO_PATH
OUTRO_PATH = paths.OUTRO_PATH
OUTPUT_ROOT = paths.OUTPUT_ROOT
RENDER_ROOT = paths.RENDER_ROOT
RESEARCH_ROOT = paths.RESEARCH_ROOT
REVIEW_ROOT = paths.REVIEW_ROOT
SCRIPT_MANIFEST_ROOT = paths.SCRIPT_MANIFEST_ROOT
SCRIPT_ROOT = paths.SCRIPT_ROOT
STORAGE_ROOT = paths.STORAGE_ROOT
TMP_ROOT = paths.RENDER_ROOT
VIDEO_ROOT = paths.VIDEO_ROOT

artifact_stem = paths.artifact_stem
artifact_stem_for_event = paths.artifact_stem_for_event
bundle_dir = paths.review_bundle_dir
research_dir = paths.research_dir
review_bundle_dir = paths.review_bundle_dir
script_manifest_dir = paths.script_manifest_dir
script_output_dir = paths.script_output_dir
